apiVersion: v1
kind: Service
metadata:
  name: {{ include "fable.fullname" . }}
  labels:
    {{- include "fable.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fable.selectorLabels" . | nindent 4 }}
